# copy to domain.env and set your domain; used by docker-compose and scripts
# frontend: travel.[domain], backend: travel-backend.[domain]
# only this file is committed; domain.env is gitignored
DOMAIN=yourdomain.com

# optional: your dev machine LAN IP for CORS when testing from phone (e.g. 192.168.1.100)
DEVSERVER=192.168.1.100
