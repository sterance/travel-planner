# frontend (vite)
VITE_API_URL=http://localhost:3000

# domain: copy domain.env.example to domain.env and set DOMAIN=yourdomain.com (single place for all domain references).
# Frontend: travel.[domain], backend: travel-backend.[domain]. Run `npm run dev:all` to generate env from domain.env and start client+server. For docker: source domain.env && docker compose ...

# backend (server) - copy to server/.env or set when running docker
# JWT_SECRET=your-secret-min-32-chars
# PORT=3000
# CORS_ORIGINS=... (script writes from domain.env; frontend origin https://travel.DOMAIN)
# DB_PATH=./data/travel.db
